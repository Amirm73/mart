import LoginP from "$/lib/ui/organisms/LoginP";
import LoginPage from "$/lib/ui/template/LoginPage";
import { copyFileSync } from "fs";
import styled from "styled-components";

const Login = () => {
  return <LoginPage />;
};

export default Login;
